name: covid_screening_app
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  async: ^2.6.1
  firebase_auth: ^3.0.1
  firebase_core: ^1.4.0
  stacked: ^2.2.3
  get_it: ^7.2.0
  cloud_firestore: ^2.4.0
  firebase_storage: ^10.0.1
  image_picker: ^0.8.2
  path_provider: ^2.0.2
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  fonts:
    - family: CenturyGothic
      fonts:
        - asset: assets/fonts/century-gothic.ttf