name: haefele_health_app
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^2.0.0
  firebase_core: ^1.3.0
  stacked: ^1.9.1
  get_it: ^5.0.6
  cloud_firestore: ^2.3.0
  firebase_storage: ^9.0.0
  image_picker: ^0.8.1+4
  path_provider: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  fonts:
    - family: CenturyGothic
      fonts:
        - asset: assets/fonts/century-gothic.ttf